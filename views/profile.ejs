<html>
    <head>
        <style>
            /* Sticky navbar - toggles between relative and fixed, depending on the scroll position. It is positioned relative until a given offset position is met in the viewport - then it "sticks" in place (like position:fixed). The sticky value is not supported in IE or Edge 15 and earlier versions. However, for these versions the navbar will inherit default position */
            .navbar {
                font-size: 25;
                overflow: hidden;
                background-color: #333;
                position: sticky;
                position: -webkit-sticky;
                top: 0;
            }

            /* Style the navigation bar links */
            .navbar a {
                float: left;
                display: block;
                color: white;
                text-align: center;
                padding: 14px 20px;
                text-decoration: none;
            }

            /* Change color on hover */
            .navbar a:hover {
                background-color: #ddd;
                color: black;
            }

            /* Active/current link */
            .navbar a.active {
                background-color: #666;
                color: white;
            }
            /* Responsive layout - when the screen is less than 400px wide, make the navigation links stack on top of each other instead of next to each other */
            @media screen and (max-width: 400px) {
                .navbar a {
                    float: none;
                    width: 100%;
                }
            }

            .card {
                box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
                max-width: 300px;
                margin: auto;
                text-align: center;
                font-family: arial;
            }

            .title {
                color: grey;
                font-size: 18px;
            }

            button {
                border: none;
                outline: 0;
                display: inline-block;
                padding: 8px;
                color: white;
                background-color: #000;
                text-align: center;
                cursor: pointer;
                width: 100%;
                font-size: 18px;
            }

            button:hover, a:hover {
                opacity: 0.7;
            }
        </style>
    </head>
    <body>
        <div class="navbar">
            <a href="/">Home</a>
            <a href="/contact">Contact</a>
            <a href="/logout" class="right" style="float: right">Logout</a>
        </div>
        <h2 style="text-align:center">Photographer Profile Card</h2>
        <div class="card">
            <img src="photographer_avatar.jpeg" alt="John" style="width:100%">
            <h1><%= name %></h1>
            <p class="title">Expert Photographer for <%= profile %></p>
            <% if(photo_charges>0) { %>
                <p>Photo charges: <%= photo_charges %></p>
            <% }else{ %>
                <p>Photo charges Not Available</p>
            <% }if(video_charges>0){ %>
                <p>Video charges: <%= video_charges %></p>
            <% }else { %>
                <p>Video charges Not Available</p>
            <p>Contact: <%= email %></p>
            <% } %>
            <p><button onclick="handleBook('<%= username %>')">Book</button></p>
        </div>
    </body>
</html>
<script>
    function handleBook(username){
        window.location = "/book?username=" + username;
    }
</script>